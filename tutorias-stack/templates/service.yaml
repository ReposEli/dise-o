apiVersion: v1
kind: Service
metadata:
  name: {{ include "tutorias-stack.fullname" . }}
  labels:
    {{- include "tutorias-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tutorias-stack.selectorLabels" . | nindent 4 }}
