apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tutorias-stack.fullname" . }}-test-connection"
  labels:
    {{- include "tutorias-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tutorias-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
